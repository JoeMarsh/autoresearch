{
  "name": "codex-autoresearch-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "zod": "^4.3.6"
  },
  "scripts": {
    "build": "esbuild scripts/mcp-launcher.mjs --bundle --platform=node --format=esm --target=node22 --outfile=dist/mcp-server.mjs",
    "pretest": "npm run build",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
